# Azure App Service Deployment Configuration
# This file is for reference - Azure will auto-detect Node.js settings

runtime:
  name: node
  version: 18

startup:
  command: npm start

build:
  commands:
    - npm install
    - npm run build

environment:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: 8080
